# Client Environment Variables (client/.env)
VITE_API_URL=https://saathai.onrender.com

# Server Environment Variables (server/.env)
GROQ_API_KEY=your_groq_api_key_here        # For STT only (Whisper) — free tier
SARVAM_API_KEY=your_sarvam_api_key_here    # For Chat (Sarvam-M, FREE) + TTS (Bulbul v3, free ₹1000 credits on signup)
PORT=5000
